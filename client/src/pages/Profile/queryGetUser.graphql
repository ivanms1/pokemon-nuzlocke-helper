query GetUser($userId: ID) {
	user: getUser(userId: $userId) {
		id
		name
		nuzlockes {
			name
			game {
				name
			}
			team {
				pokemon {
					name
					sprite
				}
				nickname
				status
			}
		}
	}
}
