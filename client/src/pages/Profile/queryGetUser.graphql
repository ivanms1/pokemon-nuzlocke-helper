query GetUser($userId: ID) {
	user: getUser(userId: $userId) {
		id
		name
		nuzlockes {
			_id
			name
			game {
				name
			}
			team {
				pokemon {
					_id
					name
					sprite
				}
				nickname
				status
			}
		}
	}
}
